apiVersion: v1
kind: ConfigMap
metadata:
  name: inventory-service-config
data:
  INVENTORY_DB_NAME: "restaurant_inventory"
  INVENTORY_DB_URL: "jdbc:mysql://mysql:3306/restaurant_inventory"
  INVENTORY_EVENTS_TOPIC: "inventory.events"
  INVENTORY_RESERVATION_TTL_SECONDS: "900"
